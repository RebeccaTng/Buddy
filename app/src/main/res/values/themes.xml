<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Buddy" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Don't allow dark mode. -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomAlert" >
        <item name="android:background">@color/beige</item>
        <item name="android:fontFamily">@font/mulish_regular</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Bluetooth -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:fontFamily">@font/mulish_extrabold</item>
        <item name="android:textColor">@color/beige</item>
        <item name="android:textSize">26sp</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" >
        <item name="android:fontFamily">@font/mulish_extrabold</item>
        <item name="android:textColor">@color/beige</item>
        <item name="android:textSize">26sp</item>
    </style>

    <style name="BlufiActivityButtonBarButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_marginStart">35dp</item>
        <item name="android:layout_marginEnd">35dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:background">@drawable/bg_btn_main</item>
        <item name="android:fontFamily">@font/mulish_extrabold</item>
        <item name="android:textColor">@color/beige</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>